@model Entites.Models.GameModels.GameGenre

@{
    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="box box-primary">
    <div class="box-header with-border">
        <h3 class="box-title">Game Genre Ekle</h3>
    </div>
    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="text-danger">
            @ViewBag.Message
        </div>
        <!-- form start -->
        <form role="form">
            <div class="box-body">
                @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                <div class="form-group">
                    @Html.LabelFor(model => model.GenreName, htmlAttributes: new { @class = "control-label col-md-2" })
                    <div class="col-md-12">
                        @Html.EditorFor(model => model.GenreName, new { htmlAttributes = new { @class = "form-control" } })
                        @Html.ValidationMessageFor(model => model.GenreName, "", new { @class = "text-danger" })
                    </div>
                </div>

                <div class="form-group">
                    <div class="col-md-10" style="margin-top:10px;">
                        <input type="submit" value="Game Genre Oluştur" class="btn btn-primary" />
                    </div>
                </div>
            </div>
        </form>
    }
</div>

@Html.ActionLink("Listeye Geri Dön", "Index")




@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
