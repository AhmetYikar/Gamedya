@model Entites.Models.GameModels.GameGenre

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="box box-primary">
    <div class="box-body box-profile">
        <h3 class="profile-username text-center">Game Genre Sil</h3>
        <p class="text-muted text-center text-danger">GAME GENRE SİLMEK İSTEDİĞİNİZE EMİN MİSİNİZ?</p>
        <div class="text-danger">@TempData["Message"]</div>

        <ul class="list-group list-group-unbordered">
            <li class="list-group-item">
                <dt>
                    @Html.DisplayNameFor(model => model.GenreName)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.GenreName)
                </dd>
            </li>
        </ul>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color">
                <input type="submit" value="Sil" class="btn btn-primary btn-block" />
            </div>
        }
    </div>
</div>
@Html.ActionLink("Listeye Geri Dön", "Index")

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
